---
description: 
globs: *.java
alwaysApply: false
---
SpringBoot版本3.3.5
1.使用import static com.ksptool.entities.Entities.assign; assign(source,target)映射对象的同名属性,这是一个可靠的映射函数,它能处理绝大多数类型转换，包括Date到String
2.不允许三元表达式,使用普通IF
3.需要为接口创建单独的Dto用于接收数据，Vo用于响应数据,所有的Controller的输入都是Dto而响应都为Vo
4.后端校验参数使用HibernateValidation注解
5.字段注释按类角色分层：
  - Dto/Vo：必须使用Swagger @Schema(description="...")注解
  - Po：使用org.hibernate.annotations.Comment注解
  - 普通结构体（Session、Context等内部类）：单行//注释
  - 方法统一使用Javadoc（/** */）
6.尽可能少使用StreamAPI,除非必须使用它.
7.使用多个if-return组合或多个if代替else
8.Service中的简单查询统一使用Example，复杂查询使用JPQL投影到一个单独的Vo

## Controller开发规范
1.Controller类使用@RestController注解，统一使用@RequestMapping定义路径前缀
2.所有接口方法都使用@PostMapping注解，不使用GET请求
3.接口方法参数统一使用@RequestBody @Valid接收Dto对象
4.接口方法返回值统一使用Result包装，成功时使用Result.success()
5.可能抛出业务异常的方法声明throws BizException
6.Service字段使用@Autowired注解进行依赖注入
7.接口方法添加简洁的javadoc注释说明功能
8.成功响应消息使用中文描述，如"更新用户状态成功"

## Service开发规范
1.Service类使用@Service注解，不需要创建接口，直接实现
2.Repository字段使用@Autowired注解进行依赖注入
3.数据修改操作的方法必须添加@Transactional注解
4.方法参数校验在方法开始时进行，参数为空或无效时抛出BizException
5.使用Example进行简单查询，格式：new Po(); query.setField(value); repository.findOne(Example.of(query))
6.数据转换统一使用assign(source, target)函数，但需注意其局限性：只能处理同名字段映射(支持简单类型转换)，字段名不同或需要复杂转换逻辑时需手动处理
7.业务异常信息使用中文描述，如"用户不存在"
8.编辑数据时先查询验证存在性，再检查唯一性约束
9.删除数据时先查询验证存在性
10.批量操作时使用repository.findAllById()和repository.saveAll()提高效率
11.提供数据校验方法时，返回详细的操作结果说明
12.方法注释使用javadoc格式，说明参数、返回值和异常情况
13.业务逻辑复杂时使用多个if判断替代else，提前return减少嵌套

## Repository开发规范
1.Repository接口使用@Repository注解，继承JpaRepository<Po, Long>
2.自定义查询方法使用@Query注解，JPQL语句必须使用多行字符串(三引号)格式
3.查询参数使用@Param注解明确标注参数名称
4.复杂查询直接投影到Vo对象，格式：SELECT new com.xxx.Vo(字段列表) FROM Po
5.查询条件支持动态过滤，使用(:param IS NULL OR 条件)模式处理可选参数
6.更新操作使用@Modifying注解标注
7.手动编写@Query的JPQL方法命名必须以get开头，如getAdminModelVariantList、getGroupList
8.Repository方法添加简洁的javadoc注释说明查询目的和返回内容

## 函数命名规范
1.Controller方法：get+业务对象+功能描述，如getModelVariantList、getModelVariantDetails
2.Controller操作方法：动词+业务对象，如saveModelVariant、removeModelVariant、toggleModelVariant
3.Service查询方法：get+使用场景+业务对象+功能，如getClientModelVariantList、getAdminPlayerList
4.Service业务方法：动词+业务对象，如validateSystemModelVariants、detachAllActivePlayersByUserId
5.方法名使用驼峰命名法，动词在前，名词在后，含义清晰明确

## 变量命名规范
1.数据传输对象统一命名：dto（请求参数）、vo（响应数据）、po（数据库实体）
2.分页查询结果：pageResult、page（Page类型）
3.集合变量：复数形式，如vos、modelVariants、permissions
4.查询对象：query（用于Example查询）
5.计数变量：使用Count后缀，如addedCount、existCount、maxSeq
6.ID类型变量：实体名+Id，如userId、playerId、groupId
7.业务字段变量：使用完整单词，避免缩写，如keyword、enabled、username
8.临时变量使用有意义的名称，避免使用单字母变量（循环变量除外）

9.布尔类型变量使用is/has/can等前缀，如isSystem、hasPermission