---
alwaysApply: true
---

# 项目索引

本项目是一个**前后端一体**的基础框架系统。

## 后端

- 语言：Java
- 位置：`src/main/java/`
- 根包：`com.ksptool`
- 构建工具：Maven（`pom.xml`）

## 前端

- 技术栈：Vue 3 + TypeScript + Vite
- 位置：`src/main/resources/web-ui/`
- 源码目录：`src/main/resources/web-ui/src/`
- 包管理：pnpm

## 代码规范

- 元规范：@.cursor/rules/_meta/RuleSkeleton.mdc
- 术语表：@.cursor/rules/_meta/Glossary.mdc
- 后端规范：@.cursor/rules/web-service/WebService.mdc
- 后端 Dto 规范：@.cursor/rules/web-service/module/WebServiceDtoMR.mdc
- 后端 Vo 规范：@.cursor/rules/web-service/module/WebServiceVoMR.mdc
- 后端 Controller 规范：@.cursor/rules/web-service/module/WebServiceControllerMR.mdc
- 后端 Service 规范：@.cursor/rules/web-service/module/WebServiceServiceMR.mdc
- 后端 Repository 规范：@.cursor/rules/web-service/module/WebServiceRepositoryMR.mdc
- 后端方法尺度与控制流规范：@.cursor/rules/web-service/module/WebServiceMethodMR.mdc
- 前端通用规范：@.cursor/rules/web-ui/WebUI.mdc
- 前端 Api 规范：@.cursor/rules/web-ui/WebUiApi.mdc
- 前端 Service 规范：@.cursor/rules/web-ui/WebUIService.mdc
- 前端 SFC↔Service 边界规范：@.cursor/rules/web-ui/WebUiSfcServiceCR.mdc

## AI 禁止项（用户无明确指示时严禁执行）

- **禁止拆分函数**：后端 Java 方法不得拆成多个 private 辅助函数，前端不得将 Vue SFC `<script>` 或 Service 中函数拆分成多个小函数
- **禁止新建组件**：不得创建新的 `.vue` 组件文件，模板逻辑能写在一个 SFC 内就写在一个 SFC 内
- **禁止拆分脚本**：SFC 模板逻辑不可写入单独的 `.ts` 文件，所有逻辑保持在 SFC 的 `<script setup>` 内
- **禁止抽取全局/公共模块**：不得新建共享变量、公共工具函数、全局常量、公共类型文件，变量随用随定义在最近作用域

## 通用禁止项

- 所有注释（Java `//` `/* */`、TS/JS `//` `/* */`、HTML `<!-- -->` 等）中严格禁止数字序号（`1.` `2.` `3.`）或中文顺序词（`先` `后` `再` `然后` `首先` `其次` `最后` 等）
- 注释格式：`//直接写注释`，`//` 后无空格，禁止使用 `----` 分隔线
- 禁止 `switch`、`else`
- Java 字符串判空使用 `StringUtils`（Apache Commons Lang3）
- 不使用全类名，统一 `import` + 简单类名
