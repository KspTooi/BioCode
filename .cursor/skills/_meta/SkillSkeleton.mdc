---
description: 元规范：所有 .cursor/skills/**/SKILL.md 文件必须遵循的骨架与约定
globs:
alwaysApply: false
kind: meta
---

# Skill 骨架（SkillSkeleton）

本文件是所有 Skill 文件的**唯一元规范**。新建或修改任何 `.cursor/skills/**/SKILL.md` 前必读。

---

## 1. Skill 与 Rule 的区别

| 维度 | Rule（规则） | Skill（技能） |
| --- | --- | --- |
| 回答的问题 | "这个文件/模块**怎么写**" | "这个功能/组件**怎么用**" |
| 触发方式 | `globs` 自动匹配 | 用户手动 `/skill-name` 或 AI 按 description 判断 |
| 核心内容 | 编码约束、模板、反模式 | 选型指南、接入步骤、参数契约、陷阱 |
| 载体 | `*.mdc` 文件 | `SKILL.md` 文件（每个 Skill 一个独立目录） |

---

## 2. 目录结构

```
.cursor/skills/
├── _meta/
│   └── SkillSkeleton.mdc   ← 本文件（元规范）
├── web-service/             ← 后端 Skill
│   └── {skill-name}/
│       └── SKILL.md
└── web-ui/                  ← 前端 Skill
    └── {skill-name}/
        └── SKILL.md
```

---

## 3. 文件命名规范

| 类型             | 路径格式                              | 示例                                       |
| ---------------- | ------------------------------------- | ------------------------------------------ |
| web-service Skill | `web-service/{skill-name}/SKILL.md`   | `web-service/excel-export/SKILL.md`        |
| web-ui Skill     | `web-ui/{skill-name}/SKILL.md`        | `web-ui/org-selector/SKILL.md`             |

- `{skill-name}` 必填，kebab-case，与 frontmatter `name` 一致
- 每个 Skill 独占一个目录，目录内只有 `SKILL.md`（资源文件除外）

---

## 4. Frontmatter 必填字段

```yaml
---
name: <kebab-case 技能名，与目录名逐字一致>
description: <一句话描述何时使用本 Skill，包含触发关键词>
---
```

- `name` 必填，kebab-case，与父目录名逐字一致
- `description` 必填，必须包含触发关键词（如"选择器/导出/上传"），AI 据此判断何时自动加载
- Skill 无 `kind` / `globs` / `alwaysApply` 字段

---

## 5. Skill 骨架（固定 6 节）

> 章节编号与标题**逐字固定**，不得增删或改名。

```markdown
## 1. 选型

## 2. 快速接入

## 3. 参数契约

## 4. 模板（Templates）

## 5. 陷阱（Traps）

## 6. 源码索引
```

### 各节写什么

**§1 选型**

- 一张 Markdown 表格，列出"业务诉求 → 使用哪个"
- 表格至少包含：业务场景、对应组件/方案、关键区别
- 若只有一个选择（无需选型），直接写"统一使用 Xxx，无替代方案"

**§2 快速接入**

- 最小可跑通的接入步骤，≤ 5 步
- 每步只写一行，不展开解释
- Import 路径必须写完整绝对路径（`@/views/...`）

**§3 参数契约**

- 固定用表格列出 Props/参数/配置项
- 表头：`| 参数 | 类型 | 必填 | 默认值 | 说明 |`
- 复杂类型用 `XxxVo` 占位并附类型定义链接，不在本节展开

**§4 模板（Templates）**

- 按"场景 → 完整可复制代码"组织，每个场景一个 `###` 小节
- 模板代码必须可直接复制使用，变量用 `xxx / Xxx` 占位
- 每个模板必须标注 `v-model` 类型和必填事件回调

**§5 陷阱（Traps）**

- 常见错误写法与正确写法对照
- 每条 ≤ 5 行代码，用"❌ / ✅"标注
- 不解释"为什么错"——只展示错在哪

**§6 源码索引**

- 列出本 Skill 涉及的所有源码文件路径（绝对路径）
- 每行一个文件，附一句话说明其职责

---

## 6. 强制约束

1. Skill 全文代码块行数不限（与 Rule 不同），但每个模板必须独立可运行
2. `description` 必须包含至少 2 个触发关键词，确保 AI 能在正确的场景自动加载
3. 禁止在 §3 参数契约中写实现细节——参数表只声明契约，不解释内部逻辑
4. 禁止在 Skill 中描述"这个组件是怎么实现的"——Skill 只管使用，不管实现
5. 禁止出现"建议"、"尽量"、"最好"等模糊措辞
6. 每个 Skill 总行数控制在 300 行以内
